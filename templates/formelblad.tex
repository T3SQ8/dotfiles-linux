\documentclass[a4paper]{article}

\usepackage{parskip}
\usepackage[margin=0.5in]{geometry}
\pagenumbering{gobble}

%\formel[<comment>]{<name>}{<formula>}
\newcommand{\formel}[3][]{
	\textbf{#2:}\\ #3
	\ifx #1\undefined \else
	{\tiny(#1)}
	\fi
}

\begin{document}

\end{document}
