#!/bin/sh

[ ! "${PWD##*/}" = "backups" ] && echo "Wrong directory" && exit

backup_dir="$(date +%Y-%m-%d)" 
mkdir "$backup_dir" || exit 1
cp -rv "$HOME" "$backup_dir"
echo "Syncing..."
sync
echo "Done"
