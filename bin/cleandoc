#!/usr/bin/awk -f

/^\s*$/ && NR==1 {print "First line empty"}

/^ / {print "Spaces BOL:", NR}

/\s$/ {print "Whitespace EOL:", NR}

/ {2,}/ {print "Multiple spaces", NR}

/ \t|\t / {print "Mismatched tabs and spaces:", NR}

END { if(match($0, /^\s*$/)) print "Last line empty" }
