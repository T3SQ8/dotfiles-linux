#!/bin/sh

file="$(date +%Y-%m-%d).tar.gz"

[ "${PWD##*/}" != "backups-computer" ] && { echo "Wrong directory"; exit 1 ;}

pacman -Qqe > "$HOME/installed-packages.txt"
tar -cvzf "$file" -X ~/.local/backups-ignore "$HOME"
md5sum "$file" >> checksums.md5
echo "Syncing..."; sync
